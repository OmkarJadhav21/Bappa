import { LoginDirDirective } from './login-dir.directive';

describe('LoginDirDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginDirDirective();
    expect(directive).toBeTruthy();
  });
});
